# Python
__pycache__/
.pytest_cache/
.venv/

# Node — test-only headless-browser harness scaffolding (not part of the app)
node_modules/
package.json
package-lock.json
tools/

# Licence branch: the DB and source parquets are NOT redistributed. The DB is
# generated client-side from data.gouv.fr (build.js) and persisted in IndexedDB.
# Local copies may sit here for dev/tests but must not be committed/served.
database/*.duckdb
database/*.parquet
